name: Angular Build
'on': push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install -g @angular/cli
      - run: npm ci
      - run: ng b
        with:
          token: ${{ secrets.TOKEN }}