version: '3'
services:
    front:
        image: file-sync-front
        networks:
            - my-network
        ports:
            - "80:80"
        depends_on:
            - api
    api:
        image: file-sync-api
        networks:
            - my-network
        ports:
            - "3001:3000"
networks:
    my-network:
        driver: bridge
